{
  "name": "cypress-ai-assert",
  "version": "1.0.0",
  "type": "module",
  "description": "Cypress custom command to validate text with LLMs (Anthropic, OpenAI, etc).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "demo:open": "npm run build && cypress open",
    "demo:run": "npm run build && cypress run"
  },
  "keywords": ["cypress", "llm", "testing", "ai"],
  "author": "Alexander Roy",
  "license": "MIT",
  "devDependencies": {
    "@types/cypress": "^1.1.6",
    "@types/node": "^24.3.0",
    "cypress": "^15.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
